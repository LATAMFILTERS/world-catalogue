import { EngineEntry, EngineId } from './master-data-types';

/**
 * ENGINE REGISTRY
 * 
 * Master registry of all engines supported by ELIMFILTERS.
 */
export const ENGINE_REGISTRY: Record<EngineId, EngineEntry> = {};
