import { EquipmentEntry, EquipmentId } from './master-data-types';

/**
 * EQUIPMENT REGISTRY
 * 
 * Master registry of all machinery and equipment supported by ELIMFILTERS.
 */
export const EQUIPMENT_REGISTRY: Record<EquipmentId, EquipmentEntry> = {};
